node_modules
target/
dist/
*.wasm
!evals/**/*.golden.wasm
# pre-alpha: ship the built engine so the playground runs without a Rust toolchain
!packages/core/wasm/instruments_dsp.wasm
.DS_Store
*.log
__pycache__/
*.py[cod]
evals/incumbents/renders/
.claude/worktrees/
.worktrees/

# Staged into packages at build time; the repo root owns the originals.
packages/*/LICENSE-MIT
packages/*/LICENSE-APACHE

# npm pack output from the release checks (they clean up unless killed mid-run)
packages/*/*.tgz
