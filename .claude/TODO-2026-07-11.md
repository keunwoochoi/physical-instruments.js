# instruments.js — rolling backlog + decision log

> After every merged PR: mark items `[x]` with root cause → fix → verification (numbers) → review outcome. Add new Open/Backlog items surfaced. (`AGENTS.md`, `skills/finalize-pr`.)

## Decision log

- **2026-07-11 — Project founded; harness bootstrapped (pre-PR era).**
  Name: `instruments.js` (plural; "vst.js" disqualified — Steinberg trademark forbids VST blends/derivatives + mis-signaling; plural avoids Sentry's literal `instrument.js` filename collision and the observability verb reading; full survey in the approved plan). License: dual MIT/Apache-2.0. Clean-room: papers-only for copyleft (`agentic-docs/licensing.md`). Eval panel: Keunwoo + Hayoung + 3–5 community, public at v0.5. Architecture: single-worklet Rust→WASM, SoA voices, no COOP/COEP (`agentic-docs/design/2026-07-11-architecture.md`). Multi-track arrangements are the unit of performance budgeting (PRINCIPLES #4) — added at founding by Keunwoo.

- **2026-07-11 — First sound: issues #3/#5/#6-lite/#8-lite/#10 landed in one vertical slice (branch `feat/first-sound`).**
  Root cause of "nothing playable": no engine existed. Built: Rust DSP core (modal mallets ×4, EKS plucked guitar/bass, tine e-piano, GM drum kit, polyBLEP subtractive pad; voice pool w/ stealing, track buses w/ smoothed equal-power pan, master soft-clip, denormal flushing, per-instance SR) + hand-rolled WASM ABI (no bindgen) + AudioWorklet processor (main-thread compile → postMessage → sync instantiate; quantum-boundary event queue; 1 Hz stats) + implemented `createEngine()`/tracks/`play()`/`renderOffline()` + playground (keyboard, drum pads, 6-track demo arrangement, offline WAV download) + Node verification harness.
  **Verification (numbers):** 9/9 cargo tests incl. tuning at 44.1k & 48k (440 Hz ±3%), damping, stealing, pad release; demo render 21.7 s, peak 0.64, RMS −23 dB, 0 NaN, 21 voices, **29.6 µs/quantum = 1.11% of the 2.67 ms budget** (Node/wasm); sustained-retrigger bench 43.8 µs = 1.64%. WASM **18.2 KB gzipped**. Human listening: pending (Keunwoo — this is the Q1 AB-gate input, n≥5).
  **Decision (Keunwoo, mid-build): no paradigm purity** — classic subtractive/FM voices welcome where fast+beautiful wins (PRINCIPLES #5 amended; SynthPad added, now backing strings/synth/brass/voice families instead of mallet placeholders).
  Known honest gaps → Open: quantum-boundary (≤2.7 ms) event jitter, not sample-offset; iOS Safari untested; bundler matrix untested; piano family maps to e-piano placeholder.

- **2026-07-11 — Silence bug fixed + MIDI release + first full 7-persona panel review (PR #11).**
  Root cause of Keunwoo's "things don't sound": structured-cloning a WebAssembly.Module into an AudioWorklet is silently dropped to `messageerror` on Safari/Chromium-headless (found via headless step-probe after MIME/instantiation ruled out). Fix: bytes-only init + in-worklet sync compile; offline renders deliver bytes+schedule via processorOptions (port messages aren't pumped around offline render loops — the earlier handshake await was a deadlock). Never-silent guards added (onmessageerror/onprocessorerror/red status).
  Shipped same session: @instrumentsjs/midi SMF parser (tempo maps/running status/GM/CC64), engine sustain pedal, sample-accurate segment scheduling, batched events, playground MIDI drag-drop + live Web MIDI, Playwright E2E in CI.
  **Panel review round 1 (all 7 personas, PR #11)** — fixed same-day: subarray alloc + O(N²) shift on render path (Yotam); guitar 44.1k test + render-harness divergence from shipped path + both-channel stats (Keunwoo); discontinuous soft-clip knee → slope-continuous limiter + e-piano drive key-tracked down (producer); KS loop-filter phase-delay tuning compensation — strings no longer detune with velocity — + allpass conditioning (Juhan, verified by new velocity-independence test); swing/micro-timing/composed melody contours/dynamic arc in demo + vibraphone rotary tremolo (Hayoung); honest bundler claims + ~24 KB all-in number (senior-web-dev); e2e live-path escape hatch removed. Deferred → issues: MPE/continuous control (Jordan blocking), e-piano ADAA/oversampling, sympathetic resonance/pedal bloom, drum vel→timbre, steal crossfade, bundler-matrix fixtures, FFT-grade tuning eval.
  Verification post-fixes: 11/11 cargo (incl. velocity-independent tuning), render 1.22% budget, E2E PASS (no escape hatch), 0 errors.
  Customer-zero note: integration branch in music-transcription-app was externally reverted (other work in flight there) → preserved as `demos/customer-zero/` drop-in example instead; hands off that repo.

- **2026-07-11 — Acoustic piano + measured loudness normalization (Keunwoo requests, same evening).**
  Root cause of "piano too loud and kinda weird": the piano family mapped to the e-piano placeholder, whose tanh pickup boosts small signals up to ~12 dB — plus a measured **36 dB loudness spread** across families (bass +8.7 dB over marimba, glockenspiel at peak 0.005 ≈ inaudible). Fixes: (1) `PianoVoice` — 1/2/3 detuned string waveguides by register (unison beating), cascaded dispersion allpasses (inharmonicity) with DC-delay-compensated tuning, velocity-brightened hammer excitation + strike comb, register-scaled t60 (11 s bass → 0.9 s top), damper on note-off, pedal-integrated; piano family now maps to it everywhere, playground defaults to it. (2) `makeup_gain` per family, MEASURED via new `scripts/dev/measure-loudness.mjs` — post-fix all families sit at −26 dB ±0.2 (glock/musicbox HF-discounted on purpose); demo/render track gains re-leveled to musical values. Also: bass preset warmed (darker loop, neck-position pluck) per the listening note. Test upgrade: autocorrelation pitch estimator with parabolic refinement replaced zero-crossing counting (which mis-read piano's partial-rich tail as 668 Hz — exactly the Juhan panel prediction); piano verified in tune ±1.5% at both sample rates AND velocities.
  Verification: 14/14 cargo; loudness table flat; demo render peak 0.735 / RMS −26.3 / 0.97% budget; E2E PASS, 0 errors. Human listening: Keunwoo, playground is hot-reloaded — hard refresh.

- **2026-07-11 — Piano v2 + BS.1770/pyloudnorm loudness normalization (Keunwoo: "piano sucks", "use pyloudness").**
  Piano v2 fixes the three classic KS-piano failures: (1) pluck-like attack → hammer excitation = raised-cosine displacement pulse (width = fraction of string period, velocity-narrowed) blended with velocity-brightened noise; (2) single-stage decay → two-stage via string-pair asymmetry (dark long sustain string fed darker excitation; bright short bloom strings, bloom length velocity-scaled) per Weinreich polarization/coupling; (3) airless → 3-mode body-knock bank (85/172/318 Hz). Dispersion retuned down in bass ("watery"). Steering tool: `piano-audition.mjs` (two-stage t60 ratio, Hann-windowed spectral-centroid trajectory + audition WAV). **Measurement-tool lesson: the first centroid metric (unwindowed, coarse grid) reported RISING centroids that a peak analysis disproved (and a decimated debug DFT manufactured 23 kHz mirror ghosts) — instrument the instruments with the same rigor as the DSP.** Post-fix: two-stage ratios 3–7 at mf/ff, centroid falls bright→mellow, velocity→brightness strong (C2 307→774 Hz).
  Loudness: RMS proxy replaced with **pyloudnorm (BS.1770 integrated LUFS)** — `measure-loudness.mjs --wav-dir` + `measure-loudness.py`. K-weighting corrected the RMS lies: piano was +4.4 LU hot (user's ear right, twice), glock needed 4.7× MORE than the RMS-guess. Post-bake: **all 10 families at −26.2 LUFS ±0.1**.
  Verification: 14/14 cargo, LUFS table flat, demo 1.03% budget, E2E PASS 0 errors. Human listening: piano-audition.wav + instruments-demo.wav in scratchpad.

- **2026-07-11 — Piano v3 (real hammer collision), ride fix, 3 guitars + ADAA amp, research-venues doc, match-reference skill.**
  Keunwoo: "piano sounds like harpsichord — be SoTA." Correct diagnosis: pre-filling the delay line IS the pluck model. v3 implements the **felt hammer-string collision** (Bank/Välimäki lineage): F = K·compression^p integrated per-sample, force injected at the strike point of strings starting at REST; contact time now emerges from velocity+register. Bugs found by the metric loop: unipolar injection parks DC in the loop → in-loop DC blocker (~19 Hz); K normalized at the actual velocity pinned contact time and killed the velocity→timbre physics → normalize at reference velocity (mf). Injection gain tuned via new peak column. Post-fix: velocity→brightness real (C4 412→550 Hz), two-stage ratios 2–5+, tuning ±1.5% both rates/velocities (16/16 tests).
  Ride: sine-over-noise replaced with 7-mode inharmonic ping cluster + restrained wash (listening note: "weird tone").
  Guitars for popular music: GuitarSteel(10)/GuitarElectric(11)/GuitarDistorted(12) — PluckVoice presets + **track-bus amp stage with first-order ADAA tanh** (antialiased waveshaping, no oversampling; chords intermodulate in the amp like real electrics) + tone lowpass; in-tune through the amp, power chord bounded and singing.
  Loudness re-baked with pyloudnorm after piano/guitar changes: **13 families flat at −26.2 LUFS ±0.1** (piano v3 was −13.5 pre-correction → makeup 0.11).
  New docs: `agentic-docs/research-venues.md` (DAFx/ICMC/SMC/NIME/WAC/AES/ICASSP/ISMIR + journals + citation-trail people); `skills/match-reference` + design doc + memory: Keunwoo's **LLM auto-research loop** (render↔reference matching with physical understanding) — direction accepted, build at P1 when quality work resumes.
  Verification: 16/16 cargo, LUFS flat, demo 1.11% budget, E2E PASS 0 errors. Human listening: fresh piano-audition.wav + instruments-demo.wav; playground has the 3 new guitars.

- **2026-07-11 — First human listening verdict on piano v3 (Keunwoo): "sounds better."**
  Trajectory: v1 "sucks / harpsichord" → v3 hammer-collision "better." Not yet "great" — the
  remaining gap is the match-reference loop's job (issue #16, P1 scoped). Verdict logged per
  eval-before-trust: persona/metric gates passed, human gate now trending positive.

- **2026-07-11 (late) — Fleet launched + bundler matrix conquered + platform polish (parallel work while agents run).**
  Four fable agents running match-reference loops in isolated worktrees (piano / acoustic guitars / electrics / cymbals — Keunwoo: "all guitars suck", "ride is ridiculous", "be SoTA", full-autonomy window). Loop environment shipped first: body-resonator banks + pickup-resonance stage in the engine, render-note.mjs, compare.py (log-mel/centroid/envelope/partials/LUFS), loop-protocol runbook, NSynth CC-BY reference corpus (local-only, ledgered).
  **Bundler matrix: ALL GREEN, zero-config** — Vite 6, Next.js 15 (incl. static prerender = SSR-safety proven), raw Webpack 5, each dev+production, headlessly verified via new e2e-fixture.mjs. The panel's packaging blocker is closed with evidence; claims updated to match.
  Also: demo song v2 (steel arpeggios, electric lead, distorted tag + crash, ride timekeeping) with a shared DEMO_MIX and a new per-family `tracks` mix option on play()/renderOffline(); playground space-bar pedal / pointer-Y velocity / octave shift / 🔥 stress test; **xrun honesty** (currentFrame-gap droppedQuanta in stats + red warning); renderOffline float32 + progress; ./worklet ./wasm subpath exports; npm pack = 33 kB tarball / 6 files. GitHub Actions disabled repo-wide (Keunwoo order); accidental worktree-gitlink commit reverted + path gitignored.

- **2026-07-11 (night) — THE AUTO-RESEARCH FLEET LANDED: 4 parallel agents, all merged, every instrument reference-matched.**
  First full run of Keunwoo's match-reference loop at fleet scale — four fable agents in isolated worktrees, ~15 iterations each against real recordings. Results (log-mel distance to references, all with held-out checks): **electrics −62% tuned/−56% held-out** (velocity-sensing pickup differentiator, pickup/tone electrical refit, fret-release damping, high-gain revoice); **acoustics −40% held-out both nylon+steel** (displacement-release pick with contact-patch compliance, reference-fit body banks, steel bridge-force tap + band-limited tension modulation); **piano v4** (Stulov hysteretic felt, Weinreich prompt/aftersound roles, DC-blocker phase compensation — bass was 40 cents sharp —, soundboard radiation buildup, attack log-mel 1.36→1.01); **cymbals rebuilt** as a 22-band banded-noise engine w/ squared-bloom nonlinear-cascade gate (ride vs real CC0 ride 4.44→1.33; crash/hats on same machinery; agent fetched + license-verified its own CC0 references).
  **Convergent root-cause: THREE agents independently found the per-period loss bug** (KS loop applies loss once per period, t60_gain computed per sample → envelopes ~len× too slow, masked by dark filters). Merge strategy: acoustic's rebuild kept `PluckVoice`; electric's became `ElectricVoice`/`Kernel::EPluck` (both byte-faithful to their verified behavior); makeup rows unioned; wasm rebuilt from merged source. I migrated the orphan **bass** onto the per-period engine (all agents flagged it).
  Post-merge: **24/24 tests, LUFS re-baked flat (13 families −26.1…−26.3), demo 2.18% budget, E2E PASS.** WASM now 40 KB gz (~47 all-in) — still sample-free tiny. Auditions in scratchpad: piano-audition.wav + auditions/{guitar-nylon,guitar-steel,guitar-electric,guitar-distorted,bass,cymbal-51/49/46/42}.wav + instruments-demo.wav. **Human listening = the gate that remains.**
  Agent recommendations filed for follow-up: hi-hat choke (engine-level), ride bell GM 53 + splash 55, stiffness-dispersion allpass cascade (steel B≈6e-4), piano dispersion sign question, MAX_BODY_MODES 8→16, compare.py per-partial decay tracking + short-file guard, render-note float output, tension-mod headroom, per-block cymbal envelope stepping.

- **2026-07-11 — Familiarity ladder (Keunwoo): piano/guitar/drums carry the highest bar.** "People use them a lot, people also know them very well — our bar should be very very high." Encoded in PRINCIPLES #1. Implications: these three families get continuous loop rounds (not one-and-done), first claim on reference-quality upgrades (44.1k+ close-mic CC0 corpora — the 16 kHz NSynth ceiling and room-bleed in overhead kit refs are now the binding constraint for them), and their audition WAVs gate every release.

- **2026-07-11 — Pianoteq-class piano campaign founded (Keunwoo: "as good as the Pianoteq sound").**
  Design doc: agentic-docs/design/2026-07-11-pianoteq-class-piano.md (P1 per-key calibration → P2 soundboard/radiation → P3 longitudinal/phantom ff realism → P4 pedal vocabulary). Reference corpus staged: Salamander Grand (CC-BY, 30 keys, 48 kHz, 16 s natural decays, ledgered). **Baseline vs Salamander C4 measured**: logmel 3.25; we are too dark everywhere (centroid 260 vs 480-520 Hz); real prompt sound decays 3x faster than ours (p1 -30.7 vs -10.5 dB/s) before a LONGER singing tail (6.2 vs 4.8 s); an 85 Hz body-knock line leaks that the real piano lacks. These numbers seed P1.

- **2026-07-11 — Piano r2 human verdict (Keunwoo): "much better — maybe 1 or 2 more improvements and then pretty nice."**
  Trajectory: "harpsichord" → "better" → "much better." Round 3 scoped to the known highest-yield items: bridge coupling (r2 agent's #1 rec), Salamander brightness/decay calibration (baseline: centroid 260 vs real 480-520 Hz, prompt decay 3x slow), dispersion sign (partials stretch FLAT — should be sharp), treble late-decay crossing.

- **2026-07-11 — FLEET ROUND 2 COMPLETE: all four structural agents merged; piano r3 launched; guitar deep-research queued.**
  Merged (sequentially, each verified): **drums r2** (ride bell GM53, splash GM55, three kits pop/rock/jazz = ids 7/13/14 + TS wiring "drums-rock"/"drums-jazz", velocity-deep kick/hats, choke re-poling — kit-vs-kit metrics move toward/away from a real jazz recording exactly as intended); **electrics r2** (supply-rail sag/bloom, deterministic released-triangle pick — noise excitation was a σ=7.7 dB per-note timbre lottery —, fixed-Hz voicing refit, 15 s slow-polarization plateau, fret squeak, drive-45 singing lead; held-out −43% to 0.628); **piano r2** (12-mode soundboard cloud, band-limited quadratic phantom partials per Conklin/Bank-Sujbert, prompt-dominant Weinreich refit, transparent aftersound = biggest win, damper settle + release thud, radiation floor, air pole; tuned −34%, held-out −19%; also caught that r1's int16 renders flattered tails — float32 is now mandatory); **acoustics r2** (TRUE stiffness dispersion via per-note-solved allpass cascades made viable by eigenmode-exact excitation loading, SDL loss ladders, 16-mode bodies, pluck-off release transients, bass voicing; steel held-out −26%; BUG: GuitarSteel absent from damps list — steel never released a note).
  Post-merge: **38/38 tests, 15 families flat ±0.35 LU, demo 37 voices at 3.5% budget, E2E PASS.** Engine facilities added this round by the main loop: per-voice stereo, SympBank pedal bloom, hat choke, drum kits TS wiring, Pianoteq-campaign foundation (Salamander corpus + baseline). Auditions refreshed in scratchpad.
  In flight: **piano r3** (bridge coupling, Salamander calibration, dispersion sign, treble crossing). Queued: **guitar r3 deep-research** (Woodhouse/commuted-body/cross-string coupling — Keunwoo: "guitar is pretty far, search much better models").

- **2026-07-12 — Kick verdict (Keunwoo): jazz and rock need a COMPLETELY different sound.** Root cause: both were reference-fit against the single CC0 virtuosity kick — one drum, three re-voicings. Dedicated kick agent launched: per-kit references (CC0 search hard) + per-kit topology freedom (jazz = resonant tonal membrane w/ felt beater; rock = damped thump + beater slap forward).

- **2026-07-12 — Electrics r3 merged + THE STEEL "HI-HAT" ROOT-CAUSED AND KILLED (the artifact was also 4.6 LU of fake loudness).**
  **Electrics r3** (agent, merged fd4d4f9): clean bright-028 rig reference-fit (tuned −18%, held-out ff −21%; pickup-position comb |sin(nπq)| commuted into excitation per Zollner/Karjalainen-Smith, 2-pole contact-patch scrape replacing flat-to-Nyquist texture noise, 95 Hz Q1.4 cab HP, quadratic loop-loss key law, register-tracked pick fraction), lead authority settled at **drive 90** (sweep 18/30/55/90 — the single ADAA-tanh at 90 behaves as a limiter, so presence must be clip-GENERATED: pre-drive 1700 Hz Q3.4 mid hump + post tone 5.5 kHz), release rebuilt as measured LF finger-lift thump + banded scrape, EP-confusion now impossible on three independent axes (sustained H2–H4 above H1 vs −63 dB below; centroid 327 vs 110 Hz; note-off +10 dB burst vs fade). Gates: singing/fizz/chug/IMD all PASS; 52/52 tests; makeup union per protocol (electric 0.41 / distorted 0.21 from the agent, all other rows HEAD).
  **Steel onset impulse (fac5a64 + 4f4c0cb)**: demo clip gate (peak 1.0, adjacent-sample jump 2.0) traced to steel low-note onsets — `start_acoustic` primed the bridge/acceleration differencers from `buf[0]` BEFORE `load_carrier` rewrote the buffer, so the stale mismatch exited through both renormalized differencers as a ±full-scale 2-sample impulse on EVERY steel note. This was the user's "hi-hat playing along" tick (r3's crest fix reduced but couldn't kill it; it clipped the demo only after the +5.7 dB steel re-bake). Fixes: prime differencers + radiation HP from the FINAL carrier buffers; band-limit the contact click to the pick-scrape band (one-pole 4.2 kHz LP on the differenced noise — 16 kHz refs literally cannot see Nyquist-tilted noise, which is why the fitting loop kept rewarding it: its own note said the metric optimum "rides the clip ceiling"); quadratic click velocity law (refs' attack/body crest: ~0 dB at ff, −22 dB at pp). Post-fix: onset crest +3.2/−6.1 dB (ff/pp) vs +12 dB before; demo peak 0.947, jump 0.29; log-mel +4–7% worse — accepted, the metric was scoring the artifact (measurement-rigor lesson #4: **a metric blind above 8 kHz will actively optimize FOR ultrasonic-tilted artifacts**). Steel re-baked 0.213→0.362 (the impulse+broadband click carried 4.6 LU of K-weighted loudness); 15 families flat −20.8 ±0.7; 52/52 tests; E2E PASS; pushed.
  In flight: **kick agent** (per-kit identity, compare.py `--profile kick`). Customer-zero note: default demo file there is now `hayoung-30s.wav` (was `baram-3s.wav`) per Keunwoo.

- **2026-07-12 — KICK ROUND MERGED: three kits, three genuinely different kick drums (closing Keunwoo's "completely different sound" verdict).**
  Agent sourced its own per-kit references (all license-verified + ledgered in scratchpad SOURCES.txt): jazz = DrumGizmo DRSKit close kick (CC-BY-4.0, 70–86 Hz open bop drum, felt beater) + VSCO-2-CE concert bass drum openness anchor; rock = DrumGizmo MuldjordKit (CC-BY-4.0, ~50 Hz choked, click-forward) + Naked Drums 22" DI sub anchor; pop = existing virtuosity kick, **bit-identical digest all 13 iterations** (untouched by construction). New topologies: jazz = coupled two-head membrane ladder w/ air-loading-compressed ratios (Rossing), soft-forward undertone, amplitude-dependent damping, 10 ms felt contact, near-linear feathering law; rock = pillow-damped fast-coherent decay + quiet 1 s LF residue, highpassed beater click + 25 ms batter-head ring, compressive stomp law. Both keep the de-danced ≤6 ms tension-mod glide. 13 measured iterations on `--profile kick` (dual-res log-mel + LF view + glide + crest): tuned LF log-mel **−24…−45%**, held-out rock crest 25.5→**18.1 vs ref 18.1**; honest note: jazz held-out overall +3% while every structural axis improved (logged, accepted). Divergence at ff: fundamentals **77/47/57 Hz**, crest 9.8/16.9/19.3, beater band −43/−20/−46 dB rel LF; each render closest to its OWN reference in LF view.
  **Merge hazard logged:** `git merge` printed only the wasm conflict but kernels.rs ALSO carried markers — `git add -A` committed them and the (deny-warnings) build caught it as 2 errors with a silently stale wasm. Rule: after any agent merge, grep for conflict markers BEFORE staging; never trust the conflict list alone. Resolved as usual union (kick-round drum rows 0.59/0.35/0.60 + HEAD guitar/steel rows), amended.
  Post-merge: **54/54 tests both rates, 15 families flat −20.7…−21.0 LUFS, demo peak 0.957 / jump 0.38 / 3.95% budget, E2E PASS, MIDI green — pushed (71b7362).** Auditions: `scratchpad/auditions/kick-round/` (kick-{pop,rock,jazz}-v{25,75,120} + kit grooves; jazz groove feathers at vel 42–48). Agent recommendations filed: ModeDef native velocity-sustain scale, GM35 soft variant, compare.py `--trim` flag.
  In flight: **piano P1 per-key Salamander calibration** (launched earlier tonight).

- **2026-07-12 — PIANO P1 MERGED: the register-interpolated piano is dead; every per-key law is now a measured Salamander calibration (Pianoteq campaign phase 1 complete).**
  Agent (483k tokens, ~10 iterations): 14 `PIANO_CAL_*` tables (~1.3 KB — 24 anchor keys, 6 keys + 2 velocity layers held out) + note-start solvers: `design_piano_dispersion` (M-sweep midpoint-anchored allpass cascade ≤20 stages w/ hard ≤45%-of-loop budget guard — fixed C7 landing 380¢ flat when compensation exceeded the loop; Van Duyne–Smith/Rauhala–Välimäki), `solve_piano_loss` (two-point t60 → per-SR coefficients; unity-wire branch — a "transparent" lp_c=0.97 was silently 40 dB/s of phantom damping at treble), per-ω phase compensation, new `t60e_hi` column (prompt string's own solved HF filter — refs' prompt HF dies 3–8× faster), Stulov-range felt exponent ladder, √vel action-noise thump (C8 pp ≈ pure action noise).
  **Exit criteria measured**: tuning median **1.3¢ / max 6.7¢** over 30 keys both SRs (r3 was +10..+100¢ sharp rising); stretch shape A0–C5 median ~3¢; held-out-key late-decay **median 15% / max 38%** (fit-key misses are structural — unison-rotation re-drain floor → P2's frequency-shaped admittance); ff attack centroids median 5%; velocity spans now the refs' real 18 dB (old engine's ~5 dB was an NSynth level-normalization artifact). C4 log-mel −21% vs r3 (attack 2.44→1.43). Honest regressions logged: A1 +7% (20–60 Hz radiation excess = P2), A5–C7 n8–10 stretch bow +17..43¢ (20-stage cap; Abel–Smith sections or MAX_DISP 32 would fix).
  **Corpus incident (ledgered)**: the "staged" multi-velocity Salamander corpus was a 404 HTML page (`.tar.xz` vs freepats' `.tar.gz`) — agent fetched the real CC-BY-3.0 V3 FLAC pack and extended the ledger row. Lesson: verify reference corpora are actual audio at staging time, not at fit time.
  **Cost flag**: 8 sustained piano voices 110.8 µs/quantum (r3 ~54) — the dispersion cascades; full demo **6.37% of budget** (was 3.95%), init ≤320 µs worst. Within all gates; the 40 µs/instrument fleet guideline is now formally piano-exempt until P2 revisits.
  Post-merge: **54/54 tests both rates, 15 families flat −21.0…−20.7 (piano re-baked 0.130→0.066 — per-key trims raised the mid +5.9 LU), demo peak 0.952, E2E PASS, MIDI green — pushed (c19d3ae).** Merge was clean (wasm-only conflict; full marker sweep passed — new rule held). Auditions: `scratchpad/piano-p1/auditions/` (A0→C8 ladder pp/mf/ff, pedaled arpeggio, C4/A1/C#5 singles vs `piano-r3/final/` counterparts) + hayoung-30s re-rendered through the P1 piano in `auditions-0712/`.
  P2 mandate seeded by P1 findings: soundboard owns 20–60 Hz bass radiation excess (+15 dB at A0 attack), frequency-shaped bridge admittance (deep-bass prompt/tail conflict), pp excitation spectrum, slow-polarization 40 s tail. Also filed: measure all 88 keys for level voicing (pipeline ready — F#3's −6 dB dip is real Salamander voicing, unreachable by interpolation); per-key 3-point velocity curve.

- **2026-07-12 — KEUNWOO LISTENING PASS → metric upgrade + round-4 fleet (three parallel agents).**
  Verdicts (verbatim-in-spirit): piano "overall good, the noise/stomping part is too strong"; acoustic guitars "need more body/neck resonance" + add "acoustic" to the names; electrics "need more 'amplifier' factor — notes remain longer, clean AND distorted"; pop kick "reduce tonality" + "differentiate open hat vs ride more"; rock kit "too similar with the pop kit"; jazz kick "crazy clear tonality — should be very subtle", snare "nice", hat "too hard", others → brushes. Plus: **similarity must use perceptual frequency weighting.**
  Done inline (eb815f6, 35a52c1): `compare.py` log-mel now **K-weighted (BS.1770) by default** — per-mel-band weights at band centers, floor 0.2, mean-normalized; LF zoom views stay flat by design (they inspect what the weighted view de-emphasizes); `--flat-weighting` for historical continuity. Piano thump trimmed −4.4 dB (law shape kept; residual bass "stomp" = the P2 radiation item). Playground labels now "Acoustic guitar (nylon/steel)"; core gains `guitar-acoustic[-nylon/-steel]` group aliases (canonical names unchanged). 54/54, demo peak 0.952, pushed.
  Launched (parallel worktrees, strict regions): **acoustic-body agent** (Helmholtz air resonance + coupled top pair per Christensen–Vistisen, mid-body modes, body ring-out, Woodhouse mode tables; AcPluck+body region), **electric-amp agent** (amp compression/sustain envelope-flattening, clean sag-recovery vs distorted limiter-hold, r3's post-drive cab/presence biquad rec now in scope; ElectricVoice/amp region), **drums agent** (all six kit verdicts incl. rock full-kit divergence vs MuldjordKit articulations + jazz brush treatment kit-wide except the praised snare; drums region). All fit on the K-weighted metric.

- **2026-07-12 — Keunwoo listening round 2: acoustic pick attack + electric bass round (fifth parallel agent).**
  Verdicts: acoustic guitar "attack doesn't feel like a pick feeling, i want it to have some of it more than now" → injected into the RUNNING acoustic-body agent's brief (with the do-not-regress guard: today's onset-impulse/broadband-click fix stays; pick character = in-band 1–6 kHz scrape texture/duration/release-snap within the measured crest envelope, NOT ultrasonic tick energy). Bass: name is now "Electric bass guitar" (playground label + `bass-electric`/`electric-bass` core aliases, pushed a2de8ee) and "can be improved a lot more — attack is too soft" → dedicated bass agent launched. Structural seed verified in code: the bass arm has `click: 0.0` (zero contact transient) and no pickup-position comb despite the electric identity; agent restricted to the Bass preset arm + row + tests (AcPluck machinery is the acoustic agent's tonight; any needed struct additions must default to guitar-bit-identical and be flagged). Five agents now in flight: acoustic-body(+pick), electric-amp, drums, piano-P3 nonlinearity, electric-bass.

- **2026-07-12 — Keunwoo: piano gets string NONLINEARITY next ("non-linearity of the string's harmonics. boundary effect") — P3 pulled ahead of P2.**
  Owner reprioritization of the Pianoteq campaign: string-side nonlinear harmonic generation before the soundboard phase. Fourth parallel agent launched (piano region — free tonight): Conklin phantom partials (f_i±f_j, ff power), Bank–Sujbert transverse²→longitudinal bridge-force transduction (THE boundary effect, real-time via shaped resonators driven by squared transverse signals), tension-modulation amplitude-coupled stretch, and a check whether Fletcher-1964's clamped-boundary inharmonicity correction shrinks P1's documented A5–C7 stretch bow. Baseline = P1's honest phantom-band gap (test-comment documented). Constraints: band-limited by construction (squared signals double bandwidth), ≤+15 µs at 8 voices on top of P1's flagged 110.8 µs, P2 items excluded. Fit against Salamander ff layers at 48 kHz.

## Open

- [ ] Human listening pass on the demo (Keunwoo + Hayoung): playground + `scripts/dev/render-demo.mjs` WAV — the Q1 AB-gate input
- [ ] Sample-offset event accuracy inside the quantum (follow-up to #6)
- [ ] iOS Safari verification (gesture unlock, 44.1 kHz) — issue #5 exit criterion
- [ ] Merge `feat/first-sound` → main (human authorization required per AGENTS.md)
- [ ] Create GitHub repo + issues #1–#10 (dependency graph in the architecture doc / approved plan)
- [ ] Register npm names: `instruments.js` (primary) + `instrument.js` (alias/redirect stub) — NOTE: publish is authority-gated, human does this
- [ ] Reserve GitHub org / docs domain (`instruments.js.org` via js-org PR once a page exists)
- [ ] 30-second manual checks: X handle, VS Code marketplace ("instrumentsjs")
- [ ] `mv /Users/keunwoo/Codes/vst.js /Users/keunwoo/Codes/instruments.js` (do outside an active Claude session)
- [ ] Local toolchain: `rustup update && rustup target add wasm32-unknown-unknown` (local cargo is 1.63 / 2022)

## Backlog (Q1 issue graph)

- [ ] #1 [tracker] v1 roadmap (root)
- [ ] #2 Phase-0 harness bootstrap — mostly done locally 2026-07-11; remaining: GitHub-side setup above
- [ ] #3 Monorepo scaffold: cargo + npm workspaces, hello-wasm in CI
- [ ] #4 Public API v0 design doc: the 36-line-replacement contract (customer zero: music-transcription-app MidiPlayer.tsx)
- [ ] #5 Worklet+WASM pipeline: main-thread compile → postMessage → worklet; noteOn() plays a sine; iOS Safari verified; ABI design doc
- [ ] #6 SoA voice engine: allocation-free process(), denormal flush, param smoothing, track buses (multi-track from day one)
- [ ] #7 dsp-bench: per-voice + standard-arrangement cost vs 2.67ms budget; CI tripwire
- [ ] #8 Modal resonator bank + mallet exciter → marimba/vibes/bells presets (port-audit: Mutable Rings/Elements lineage)
- [ ] #9 Eval infra: fixed MIDI corpus (solo + multi-track arrangements) + incumbent renders + AB/ABX page
- [ ] #10 Offline render: OfflineAudioContext → WAV (customer-zero requirement + eval render path)
