{
  "name": "instruments.js-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Beautiful physical-modeled instruments for the browser — monorepo root",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "audit:harness": "scripts/audit/harness-audit.sh"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
