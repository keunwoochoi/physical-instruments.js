/**
 * Instrument façades and presets. First real content lands with issue #8
 * (modal mallets: marimba / vibraphone / bells) via port-audit of the
 * Mutable Rings/Elements lineage (MIT — see agentic-docs/licensing.md).
 *
 * Design rule: instruments are compositions of exciter ↔ resonator ↔ body
 * primitives (the Elements template), never monoliths.
 */

/** GM program number → instrument family. Fills out with issue #8+. */
export const GM_FAMILY_PLACEHOLDER = true;
